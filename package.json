{
  "name": "cloudvault",
  "version": "1.0.0",
  "scripts": {
    "build": "node generate-config.js",
    "start": "npx serve . -p ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
